# HSI CNN Classification (MATLAB)

This project performs hyperspectral image classification
using a Convolutional Neural Network implemented in MATLAB.

Run `main.m` to execute the full pipeline.
